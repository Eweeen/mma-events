services:
  mmaevents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mmaevents
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # env_file:
    #   - .env.production
    restart: unless-stopped
