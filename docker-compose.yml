services:
  mmaevents:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mmaevents
    expose:
      - "3000"
    environment:
      - NODE_ENV=production
    # env_file:
    #   - .env.production
    restart: unless-stopped
